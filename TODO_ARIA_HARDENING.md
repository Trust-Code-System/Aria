# Aria hardening tracker

| Status | Priority | Task | Evidence | Relevant files | Verification | Remaining risk |
| --- | --- | --- | --- | --- | --- | --- |
| [x] | P0 | **Connected-app actions were completely broken by a forced-temperature bug** (the real reason sends failed). The pinned `ai@3.4` SDK forces `temperature: 0` when a caller omits it; every 2026 model Aria uses (Claude 4.x/5.x, GPT-5/o-series) rejects it ("temperature is deprecated for this model" / "does not support 0"). So Claude — the primary action model — failed on every tool turn, then the gpt-5.6 fallback failed too, ending in `model_tool_incompatible`. | New `resolveTemperature(modelId, desired)` returns the caller's value for models that accept a custom temperature, else the accepted default `1`. Routed **every** chat/agent/report/memory/gmail model call through it. Direct API probe: opus-4-8 rejects temp 0 (400), accepts 1 (200). | `lib/ai/providers.ts`, `app/api/chat/route.ts`, `lib/ai/memory-suggest.ts`, `lib/agent/execute.ts`, `lib/agent/runtime.ts`, `app/api/reports/route.ts`, `app/api/agents/step/route.ts`, `lib/connectors/gmail.ts`, `tests/identity-and-gmail-tools.test.ts` | typecheck; 176 tests (+2); build; **two live Gmail sends succeeded** end-to-end. | Root cause is a ~2-year-old AI SDK vs. 2026 models — recommend upgrading `ai`/`@ai-sdk/*` as the durable fix. gpt-5.6 + function-tools still needs `/v1/responses` (fallback only; Claude is primary). |
| [x] | P1 | **Live Gmail SEND verified end-to-end** (the biggest previously-unverified risk). | Real send driven through Aria's own chat → Claude tool loop → approval card → owner-authorized approve → Composio execute. Two receipts `status=succeeded` with real Gmail message ids (`19f8f97929671823`, `19f8fe411c445686`), recipient/subject/body matching, `error_message=null`. Confirmed real **send** (`GMAIL_SEND_EMAIL`), not a draft. | `app/api/chat/route.ts`, `lib/connectors/chat-approval.ts`, `action_receipts` | Live browser run on localhost:3000 with the owner logged in; receipts read back from the live DB. | Owner authorized the specific recipient (abassibrahim591@gmail.com); Claude cannot log in or approve on the owner's behalf, so the approval click was owner-authorized. |
| [x] | P1 | Replace the generic chat typing dots with a turn-scoped Thinking Orbs status indicator. | `thinking-orbs@0.1.1` is installed; every NDJSON event carries the logical turn UUID; terminal, cancelled, failed, approval-gated, and timed-out turns clear the indicator. | `components/chat/thinking-indicator.tsx`, `components/chat/message-item.tsx`, `components/chat/chat.tsx`, `lib/chat/stream-protocol.ts`, `app/api/chat/route.ts` | 135 unit tests; focused desktop and mobile Playwright checks; lint; typecheck; production build. | Browser verification used an intercepted model stream, so no live provider or connector execution was exercised. |
| [x] | P1 | Fix "save this to memory" so it stores the real content, not the filler "to memory". | Referential saves ("save this", "remember this", "save that", "keep this in mind") are now a distinct `save_reference` command; the route resolves the last completed assistant reply as the fact and saves it as an approved, active memory with a "Saved to memory" confirmation + Undo card. | `lib/ai/memory-commands.ts`, `lib/ai/memory-actions.ts`, `app/api/chat/route.ts`, `tests/memory-commands.test.ts` | 13 new parser tests; full suite 148 tests; typecheck. | Live save→appears-on-Memory-page path not exercised against the running Supabase DB in this session. |
| [x] | P1 | Auto-save high-confidence memories (owner chose "auto-save" over suggest-and-approve). | `suggestMemoriesFromTurn` now stores facts with model confidence ≥ 0.7 as `approved`/`active` (source `chat_auto`), emitting a `memory_saved` "Saved to memory" + Undo card; lower-confidence facts stay `suggested`. Secrets still blocked. | `lib/ai/memory-suggest.ts`, `app/api/chat/route.ts` | typecheck; 148 tests; production build. | Auto-save path (model + Supabase insert) not exercised live this session; threshold may need tuning against real chats. |
| [?] | P1 | Verify the live save→Memory-page path (rule out workspace mismatch / silent suggestion-model failure). | Cannot inspect the running Supabase DB from here. | `app/(app)/memory/page.tsx`, `lib/ai/memory-suggest.ts` | Pending: run the app, send a fact, confirm the row appears in the selected workspace. | Empty page may still be workspace scoping or a failing suggestion model call, not code. |
| [x] | P1 | Detect and recover stuck turns so none animate/hang forever (acceptance: "no turn remains indefinitely stuck without detection"). | Turns pending/streaming past a 5-min threshold are counted in `/api/admin/health` as a warning; `POST /api/admin/health` moves them to a terminal `failed` (retryable) state. Pure classifier `isTurnStuck` unit-tested. | `lib/chat/stuck-turns.ts`, `lib/admin/system-health.ts`, `app/api/admin/health/route.ts`, `tests/stuck-turns.test.ts` | 6 new unit tests; 154-test suite; typecheck; build. | DB count/recovery queries not exercised against the live DB this session; recovery is admin-triggered, not yet scheduled. |
| [x] | P1 | Re-verify a connector's live health before exposing its tools in a chat turn (not just trusting the last refresh). | `verifyConnectionHealth` freshness-gates a live Composio probe (10-min cache), **fails open** on probe errors so a transient hiccup never breaks a working connector, and blocks tools + persists the correction only on a definitive expired/revoked/disconnected status. | `lib/connectors/health.ts`, `lib/connectors/connections.ts`, `lib/connectors/composio-session.ts`, `tests/connection-health.test.ts` | 6 new unit tests (incl. fail-open + persist); 160-test suite; typecheck; build. | Live Composio probe not exercised against the real gateway this session. |
| [x] | P1 | Authenticated e2e provable in CI, without burning model quota. | Model-free memory round-trip spec (explicit save short-circuits before any provider call) proves say→"Saved to memory"→Memory page and self-cleans; GitHub Actions workflow runs Playwright against `E2E_BASE_URL` with `E2E_*` secrets, skipping cleanly when unset. Secrets/prereqs/verify checklist documented. | `tests/e2e/memory.spec.ts`, `.github/workflows/e2e.yml`, `docs/E2E_CI_SETUP.md` | `playwright test --list` parses all 20 specs. | **Owner action:** set `E2E_BASE_URL`/`E2E_EMAIL`/`E2E_PASSWORD` per `docs/E2E_CI_SETUP.md`, then run the workflow once to confirm green. Selectors assume current UI. |
| [x] | P1 | "Add Claude" / honest quota failure. Claude was already the primary action model; the email failure was billing (OpenAI + Anthropic exhausted, Gemini can't run tools). | Action turns already route to `anthropic:claude-opus-4-8` first (routing.ts:143). Quota error is now action-aware: names OpenAI/Anthropic and explains free Gemini can't run connected-app tools. | `lib/ai/routing.ts` (verified), `lib/chat/turn-state.ts`, `app/api/chat/route.ts` | typecheck; 160-test suite; build. | Real fix for the owner is adding credits to a tool-capable provider — code cannot create quota. |
| [~] | P2 | Show provider quota in Settings. | Remaining credits are not exposed by OpenAI/Anthropic/Google via API key — that part stays skipped. Instead built the feasible signal the owner approved: live **reachability**. | `app/(app)/settings/*` | N/A | Superseded by the reachability ping below. |
| [x] | P2 | Provider-reachability ping in Settings (owner approved). | Admin-only Settings control issues a minimal live call per configured provider and shows reachable / rate-limited (over quota) / auth-failed / unreachable / not-configured, flagging when no tool-capable provider is up (why a send can't run). Pure `classifyReachability` (quota detected from error code even on a 400) is unit-tested; probes fail-soft to `unreachable`. | `lib/ai/reachability.ts`, `app/api/providers/reachability/route.ts`, `components/settings/provider-reachability.tsx`, `app/(app)/settings/page.tsx`, `tests/reachability.test.ts` | 6 new unit tests; 174-test suite; typecheck; build compiled; endpoint returns 401 unauthenticated (admin-gated). Live probe run this session showed Anthropic reachable, OpenAI+Google over quota. | Makes real (tiny, max_tokens=1) calls per check — on-demand only, admin-only. Not credits-remaining. |
| [x] | P2 | Surface silent memory-suggestion failures in system-health. | Memory-pipeline errors are logged to `error_logs` (feature_area `memory`) but suggestion failures are swallowed in chat by design, so they were invisible. `getSystemHealth` now counts non-validation memory errors in the last 24h into a `memoryErrors` metric and adds a "Memory pipeline errors" warning when > 0. | `lib/admin/system-health.ts`, `tests/system-health.test.ts` | 4 new unit tests on the pure check; 164-test suite; typecheck; build. | The 24h count query is not exercised against the live DB this session (pure classifier is tested; DB I/O follows the same untested-integration pattern as the other health counts). |
| [x] | P2 | RLS cross-user isolation guard on user-owned tables (structural safety). | A CI test parses every numbered migration and asserts (a) every `create table` gets `enable row level security`, (b) every workspace-scoped table (has `workspace_id`) is covered by an `is_workspace_member` policy — direct or loop-generated, incl. quoted policy names, and (c) `profiles` is self-scoped to `auth.uid()`. A future migration adding a workspace table without isolation now fails CI. During authoring it correctly flagged `llm_training_logs` (a parser gap on quoted policy names, since fixed — the table is properly isolated). | `tests/rls-isolation.test.ts`, verifies `supabase/migrations/*` | 4 new unit tests; 168-test suite; typecheck; build. Needs no DB/creds. | Static guard over migration SQL — proves policies are *declared*, not that the live DB has them applied (owner confirmed 0014 applied; a live two-user probe would need creds). |
